<UserControl x:Class="GraphEditor.GraphNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GraphEditor.ViewModel"
             mc:Ignorable="d"  d:DataContext="{d:DesignInstance vm:GraphNodeViewModel}"
             Height="{Binding Height}" Width="{Binding Width}"
             Canvas.Left="{Binding Y}"
             Canvas.Right="{Binding X}"
             Background="Aqua">
    <Grid>
        <TextBlock Text="Element" HorizontalAlignment="Center"/>
        <ItemsControl x:Name="IcLeftConnectors" HorizontalAlignment="Left"></ItemsControl>
        <ItemsControl x:Name="IcRightConnectors" HorizontalAlignment="Right"></ItemsControl>
    </Grid>
</UserControl>
