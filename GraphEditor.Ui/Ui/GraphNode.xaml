<UserControl x:Class="GraphEditor.GraphNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GraphEditor.ViewModel"
             mc:Ignorable="d"  d:DataContext="{d:DesignInstance vm:GraphNodeViewModel}">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Delete" Command="{Binding RemoveCommand}"/>
        </ContextMenu>
    </UserControl.ContextMenu>
    <Grid Height="{Binding Height}" Width="{Binding Width}">
        <Rectangle Fill="AliceBlue" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Rectangle.Effect>
            <DropShadowEffect BlurRadius="5" Direction="-45" ShadowDepth="2" Color="CadetBlue"/>
        </Rectangle.Effect>
        </Rectangle>
        <TextBlock Text="Element" HorizontalAlignment="Center"/>
        <ItemsControl ItemsSource="{Binding InConnectors}" HorizontalAlignment="Left">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Border>
                            <Border.Style>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="Height" Value="10" />
                                    <Setter Property="Width" Value="10" />
                                    <Setter Property="Margin" Value="-5,5,0,0"/>
                                    <Setter Property="Background" Value="CornflowerBlue" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Height" Value="12" />
                                            <Setter Property="Width" Value="12" />
                                            <Setter Property="Margin" Value="-6,4,-1,-1"/>
                                            <Setter Property="Background" Value="DeepSkyBlue" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ItemsControl ItemsSource="{Binding OutConnectors}" HorizontalAlignment="Right">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Border>
                            <Border.Style>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="Height" Value="10" />
                                    <Setter Property="Width" Value="10" />
                                    <Setter Property="Margin" Value="0,5,-5,0"/>
                                    <Setter Property="Background" Value="CornflowerBlue" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Height" Value="12" />
                                            <Setter Property="Width" Value="12" />
                                            <Setter Property="Margin" Value="-1,4,-6,-1"/>
                                            <Setter Property="Background" Value="DeepSkyBlue" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
