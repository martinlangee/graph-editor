<UserControl x:Class="GraphEditor.Ui.ConnectorButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GraphEditor.Ui.ViewModel"
             xmlns:cv="clr-namespace:GraphEditor.Ui.Converters"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:ConnectorStateViewModel}">
    <UserControl.Resources>
        <cv:CalculationConverter x:Key="CalcConverter"/>
        <cv:BytesToImageSourceConverter x:Key="BytesToImageConverter"/>
    </UserControl.Resources>
    <Grid>
        <Canvas>
            <TextBlock x:Name="_tbName" Text="{Binding Name}" Canvas.Top="{Binding ActualHeight, ElementName=_tbName, Converter={StaticResource CalcConverter}, ConverterParameter=*-0.6}" FontSize="8" Foreground="Gray">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Canvas.Left" Value="6"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsOutBound}" Value="false">
                                <Setter Property="Canvas.Left" Value="{Binding ActualWidth, ElementName=_tbName, Converter={StaticResource CalcConverter}, ConverterParameter=*-1|-7}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <Image Source="{Binding Icon, Converter={StaticResource BytesToImageConverter}}">
                <Image.Style>
                    <Style TargetType="{x:Type Image}">
                        <Setter Property="Canvas.Left" Value="-16"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsOutBound}" Value="false">
                                <Setter Property="Canvas.Left" Value="4"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>
        </Canvas>
        <Border BorderThickness="1">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <EventSetter Event="PreviewMouseLeftButtonUp" Handler="Border_PreviewMouseLeftButtonUp"/>
                    <Setter Property="Height" Value="12"/>
                    <Setter Property="Width" Value="12"/>
                    <Setter Property="Margin" Value="-7,0,0,8"/>
                    <Setter Property="Background" Value="CornflowerBlue"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Height" Value="14"/>
                            <Setter Property="Width" Value="14"/>
                            <Setter Property="Margin" Value="-8,-1,-1,7"/>
                            <Setter Property="Background" Value="DeepSkyBlue"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding IsConnecting}" Value="True">
                            <Setter Property="Background" Value="Coral"/>
                            <Setter Property="BorderBrush" Value="Red"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsConnectRequested}" Value="True">
                            <Setter Property="Background" Value="Coral"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
    </Grid>
</UserControl>
